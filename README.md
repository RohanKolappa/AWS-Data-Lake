# Real-Time-Processing-SQS
Generating mock files using mock lib in python. Creating a producer that passes messages to SQS (almost) in real time. Then SQS passes these messages to the consumer, and from there to Kinesis.
